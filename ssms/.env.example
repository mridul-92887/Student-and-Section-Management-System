# Rename this file to .env before running the app

# --- MySQL (XAMPP) connection settings ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ssms

# --- App / session settings ---
PORT=3000
SESSION_SECRET=change_this_to_a_random_long_string
